# Repository workflow

- After every completed and verified app fix, commit only the scoped changes and push the commit to the configured GitHub remote.
- Never include unrelated user changes in the commit.
- If authentication, network access, or remote policy blocks the push, report that clearly instead of treating the task as complete.
